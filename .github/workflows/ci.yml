name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches-ignore:
      - main
      - test/*

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.22.0

      - name: Set Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: pnpm

      - name: Install dependencies
        run: pnpm i

      - name: Install Test dependencies
        run: pnpm --filter test install

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test
